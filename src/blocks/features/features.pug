//- Все примеси в этом файле должны начинаться c имени блока (features)

mixin features(classes)

  //- Принимает:
  //-   classes    {string} - список классов
  //- Вызов:
        +features('some-class')

  -
    // список классов
    let allClasses = '';
    if(typeof(classes) !== 'undefined' && classes) {
      for (let item of classes.split(',')) {
        allClasses = allClasses + item.trim();
      }
    }

  section.section(class=`${allClasses.length > 0 ?`${allClasses} `: `` }features`)&attributes(attributes)
    h2.visually-hidden Преимущества
    ul.features__list
      li.features__item
        h3.section__subtitle.features__title Варим сами
        p.features__text Вот этими самыми руками. Экспериментируем и творим, что хотим.
      li.features__item
        h3.section__subtitle.features__title Своё, родное
        p.features__text Не заработка ради, а импортозамещения для. Поднимаем производство.
      li.features__item
        h3.section__subtitle.features__title Не экономим
        p.features__text Человек это то, что он ест и пьёт. У нас только качественные ингредиенты.
