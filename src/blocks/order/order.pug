//- Все примеси в этом файле должны начинаться c имени блока (order)

mixin order(text, classes)

  //- Принимает:
  //-   text    {string} - текст
  //-   classes    {string} - список классов
  //- Вызов:
        +order('Текст', 'some-class')

  -
    // список модификаторов
    let allClasses = '';
    if(typeof(classes) !== 'undefined' && classes) {
      let classesList = classes.split(',');
      for (let item of classesList) {
        allClasses = allClasses + item.trim();
      }
    }

  section.order(class=allClasses)&attributes(attributes)
    h2.order__title Заказать
    p.order__text Не ждите пока нас отрекламируют, как Вятский квас, и мы поднимем цены! Закажите настоящий крафтовый квас сейчас. При заказе от 10 баррелей — скидка 10%.
    block
